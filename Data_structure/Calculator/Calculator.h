#include <stdio.h>
#include <stdlib.h>
#include "LinkedList.h"

// 계산기 시작()
void InitCalculator();
// 괄호가 들어오면 작동하는 함수

// 우선 순위가 높은 연산자가 들어오면 처리하는 함수
// 곱하기나 나누기가 들어오면 그자리에서 바로 계산해서 노드추가
// void PriorityOperation(LinkedList *pcal, int num, int operation);

// // 헤드부터 돌아가면서 연산
// int EndCalculator(LinkedList *pcal);
